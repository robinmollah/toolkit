#!/bin/bash

# base64decode
# Usage: ./base64decode ZGRMZ3Z3ZFFNS0lkMnFkY1RKZnF0ZWxleFpKU2txTXI=

if [ -z "$1" ]; then
  echo "Usage: $0 <base64-string>"
  exit 1
fi

decoded=$(echo "$1" | base64 --decode)
echo "$decoded"
echo -n "$decoded" | pbcopy

echo -e "\nDecoded string copied to clipboard."