# This script sets environment variables from the .env file in the current directory.

if [ ! -f ".env" ]; then
  echo "Error: .env file not found in $(pwd)"
  (return 0 2>/dev/null) && return 1 || exit 1
fi

set -a
source ./.env
set +a

echo "Environment variables loaded from .env"